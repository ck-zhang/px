#!/usr/bin/env sh
set -eu

if command -v npx >/dev/null 2>&1; then
  npx --no-install commitlint --edit "$1"
else
  echo "commit-msg hook: missing 'npx' (install Node.js), or run commitlint manually" >&2
  exit 1
fi

