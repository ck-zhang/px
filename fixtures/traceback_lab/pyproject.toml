[project]
name = "traceback_lab"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool]

[tool.px]

[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"
