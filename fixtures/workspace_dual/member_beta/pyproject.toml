[project]
name = "member-beta"
version = "0.1.0"
description = "Workspace member beta"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
member-beta-cli = "member_beta.cli:main"

[tool.px]

[build-system]
requires = ["setuptools>=70", "wheel"]
build-backend = "setuptools.build_meta"
